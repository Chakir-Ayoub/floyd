import { ParaghraphiPipePipe } from './paraghraphi-pipe.pipe';

describe('ParaghraphiPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ParaghraphiPipePipe();
    expect(pipe).toBeTruthy();
  });
});
